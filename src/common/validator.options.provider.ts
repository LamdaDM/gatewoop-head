import { ValidatorOptions } from "class-validator";

export class VALIDATOR_OPTIONS implements ValidatorOptions  {
    /**
     * Implement options
     * Important one is `forbidUnknownValues: true`
     * Injectable maybe?
     */
}